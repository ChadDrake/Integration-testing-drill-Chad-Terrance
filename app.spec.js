const  app = require("../app");
const  mocha = require("mocha");
const chai = require("chai");

describe('GET/frequency', () => {
it('should return and object');

it('should return 400 if no string is sent');

it('should count the number of unique characters');

it('should return the character with the highest count in string');

it('should average the number of characters');
});



